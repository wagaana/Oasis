<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oasis.oasis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/oasis_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/oasis_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BookAppointment"></activity>
        <activity android:name=".EntryPortal" />
        <activity android:name=".AppDashboad" />
        <activity android:name=".Profile" />
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_welcome"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>